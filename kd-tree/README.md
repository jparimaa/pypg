# K-D tree

A simple implementation of the K-D tree data structure. Supported functions: add, remove, find, find minimum and find nearest. Supports also visualization which requires installing Graphviz.

![kdtree](kdtree.png?raw=true "kdtree")
